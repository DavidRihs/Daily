name: daily_server
description: HTTP server for the Daily meetings app.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.1

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  shelf_static: ^1.1.2
